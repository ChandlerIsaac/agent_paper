# 多文档研究助理 Agent 项目计划书

> 当前阶段先完成一个通用、可独立运行的 Agent 项目，不直接调用 DTI 模型，也不以 AUROC、AUPRC 等预测指标作为评价标准。项目成熟后，可复用其 RAG、工具、记忆和对话模块扩展到长尾 DTI 研究场景。

## 1. 项目名称

**ResearchMate Agent：基于 LangChain 与 Agentic RAG 的多文档研究助理**

## 2. 项目定位

面向论文阅读、资料整理和专题调研，开发一个支持多文档知识库、工具调用、多轮对话、长短期记忆和引用溯源的研究助理 Agent。

用户可以上传 PDF、DOCX、Markdown 和 TXT 文档，通过自然语言要求 Agent 查找资料、比较文献、总结方法、保存笔记以及生成带引用的研究报告。

```text
用户提出研究问题
        ↓
Agent 识别任务类型并制定检索计划
        ↓
选择本地知识库、网络搜索或笔记工具
        ↓
执行检索、重排序和证据筛选
        ↓
判断现有证据是否足够
        ├── 不足：改写查询并再次检索
        └── 充分：生成回答
        ↓
检查回答是否得到证据支持
        ↓
返回答案、引用来源及原文片段
```

## 3. 项目目标

### 3.1 学习目标

- 掌握 LangChain 1.x 的 Agent、Tool、Middleware 和结构化输出。
- 掌握 LangGraph 的状态、节点、条件路由、循环和 Checkpointer。
- 掌握文档解析、切分、嵌入、混合检索和重排序。
- 理解普通 RAG 与 Agentic RAG 的区别。
- 实现多轮对话、短期记忆和长期记忆。
- 掌握 FastAPI、SSE 流式输出、数据库和 Docker 部署。

### 3.2 工程目标

- 提供可以实际操作的网页，而不是只在 Notebook 中运行。
- 支持知识库、文档、会话和研究笔记管理。
- Agent 能自主决定是否检索以及调用哪一个工具。
- 回答必须提供可追溯的文档、页码和原文片段。
- 保存历史会话、工具调用过程和用户反馈。
- 具备测试、日志、异常处理、重试和基本评价功能。

## 4. 核心功能

### 4.1 知识库管理

- 创建和删除知识库。
- 上传 PDF、DOCX、Markdown 和 TXT 文档。
- 显示文档解析、切分、嵌入和索引状态。
- 查看文档元数据及切分结果。
- 删除文档并同步清理向量索引。
- 支持重新构建索引。

### 4.2 多文档对话

用户可以总结一篇或多篇文档，比较不同论文的方法、数据集和实验结论，查找结论依据，提取研究问题、贡献和局限性，或生成调研提纲和报告。

相同 `thread_id` 下保留上下文，使 Agent 能理解“第二篇论文”“刚才的方法”等指代。

### 4.3 Agent 工具

第一版实现以下工具：

| 工具                     | 作用                 |
| ---------------------- | ------------------ |
| `search_documents`     | 检索当前知识库中的相关片段      |
| `list_documents`       | 查询知识库中包含哪些文档       |
| `get_document_content` | 获取指定文档或页面内容        |
| `save_note`            | 将有效结论保存为研究笔记       |
| `search_notes`         | 检索历史研究笔记           |
| `generate_report`      | 根据证据生成 Markdown 报告 |

后续可以增加网络搜索、计算、报告导出以及专业数据库查询工具。

### 4.4 Agentic RAG

系统不能对所有问题都强制检索。Agent 应根据问题决定：

- 是否需要使用知识库；
- 应检索哪个知识库；
- 是否需要改写问题或拆分子问题；
- 当前检索结果是否足够；
- 是否需要第二次检索；
- 回答是否能够被检索证据支持。

### 4.5 引用溯源

每条重要结论尽量返回文档名称、页码或章节、原文片段、检索相关度和引用编号。

```text
该方法通过对比学习增强了稀疏样本的特征表示。[来源 1]

来源 1
文件：paper_01.pdf
页码：第 6 页
原文片段：……
```

### 4.6 对话与记忆

**短期记忆：**保存当前会话消息、已选择的知识库、工具结果、调研主题和临时计划，使用 LangGraph Checkpointer 按 `thread_id` 管理。

**长期记忆：**保存用户主动记录的研究笔记、关注方向、分析偏好和经过整理的历史结论。长期记忆只保存有价值的信息，不直接加载全部历史对话。

```text
短期对话记忆：理解当前会话
长期用户记忆：复用历史偏好和研究结论
RAG 文档知识库：提供可引用的外部证据
```

### 4.7 研究报告生成

1. 根据用户问题制定提纲；
2. 将提纲拆分为检索子问题；
3. 检索不同章节所需证据；
4. 对证据去重、重排序和归类；
5. 撰写带引用的 Markdown 报告；
6. 检查引用是否存在并支持对应结论。

## 5. 系统架构

```text
Vue 3 / 简单 Web 前端
        ↓ REST / SSE
FastAPI 接口层
        ↓
LangChain Agent
        ↓
LangGraph 工作流
├── 意图识别
├── 任务规划
├── 工具选择
├── 文档检索
├── 结果重排序
├── 证据充分性判断
├── 必要时二次检索
├── 引用校验
└── 回答或报告生成
        ↓
数据存储
├── SQLite / PostgreSQL：用户、会话、消息和笔记
├── Milvus Lite / Milvus Server：文档切片和长期记忆向量
└── Checkpointer：Agent 对话状态
```

建议的数据关系：

```text
User
├── KnowledgeBase
│   └── Document
│       └── DocumentChunk
├── Conversation
│   └── Message
├── ResearchNote
└── GeneratedReport
```

## 6. 技术选型

### 6.1 第一版技术栈

- Python 3.11
- LangChain 1.x
- LangGraph 1.x
- FastAPI、Pydantic 2
- SQLite、Milvus Lite、`langchain-milvus`
- PyMuPDF、python-docx
- Sentence Transformers
- pytest
- 简单 HTML 页面或 Vue 3

### 6.2 后续升级

| 第一版 | 完整版 |
| --- | --- |
| SQLite | PostgreSQL |
| Milvus Lite（本地文件） | Milvus Server（Docker） |
| 内存/SQLite Checkpointer | PostgreSQL Checkpointer |
| 简单页面 | Vue 3 完整界面 |
| Milvus 稠密向量检索 | Milvus 稠密向量 + BM25 混合检索 + 重排序 |
| 本地启动 | Docker Compose + Nginx |

Redis 不是第一版必需组件。需要缓存、限流、任务状态或分布式运行时再加入。

### 6.3 持久层职责划分

Milvus 负责向量数据及其检索元数据，不代替业务关系数据库。建议按照下面的职责拆分：

| 数据类型 | 存储位置 | 说明 |
| --- | --- | --- |
| 文档切片、Embedding 向量 | Milvus | 用于语义检索和相似度搜索 |
| `knowledge_base_id`、`document_id`、页码、来源 | Milvus metadata | 用于知识库隔离、条件过滤和引用溯源 |
| 用户、知识库、文档记录 | SQLite / PostgreSQL | 保存业务数据和文档处理状态 |
| 会话、消息、研究笔记、报告 | SQLite / PostgreSQL | 支持查询、更新和事务管理 |
| Agent 当前会话状态 | LangGraph Checkpointer | 按 `thread_id` 保存和恢复工作流 |
| PDF、DOCX 等原始文件 | 本地目录 / MinIO | 不直接存入 Milvus |

第一版使用一个 Milvus Collection，通过 `knowledge_base_id` 过滤不同知识库的数据；暂不为每个知识库单独创建 Collection。

## 7. Agent 工作流程

### 7.1 意图识别

将用户请求识别为普通对话、文档问答、文献比较、保存笔记、查询笔记或生成报告。

### 7.2 查询规划

复杂问题拆分为多个子问题，例如将“比较三篇论文的数据集、方法和结果”拆成三个检索任务。

### 7.3 检索与重排序

第一版使用向量检索；后续组合向量检索、BM25 和 Cross-Encoder，对结果进行融合与重排序。

### 7.4 充分性判断

Agent 判断现有内容能否回答问题。证据不足时，改写查询、扩大检索范围或明确说明资料不足。

### 7.5 回答与引用校验

生成回答后检查重要结论是否包含引用、引用是否支持结论、来源是否真实存在，并明确标注不确定内容。

## 8. 按开发顺序实施

> 开发时严格按下面顺序推进。每一步通过验收后再进入下一步，避免同时开发 Agent、前端、记忆和复杂检索，导致问题难以定位。

### 第一阶段：搭建项目骨架（1～2 天）

**开发内容：**

1. 创建 Python 3.11 虚拟环境和 Git 仓库；
2. 建立 `app/api`、`app/agent`、`app/rag`、`app/memory`、`tests` 和 `data` 等目录；
3. 安装 LangChain 1.x、LangGraph 1.x、FastAPI、Milvus Lite、`langchain-milvus`、PyMuPDF 和 pytest；
4. 建立 `.env`、配置类、日志配置和统一异常类；
5. 单独测试大模型和 Embedding 模型是否能够正常调用。

**阶段产出：**项目可以启动，能够在命令行向模型发送一句话并获得回复。

**验收标准：**配置中不硬编码 API Key；重新创建环境后可以根据依赖文件成功运行。

### 第二阶段：完成文档解析与入库（2～3 天）

**开发内容：**

1. 先支持 PDF，读取正文并保留文件名、页码等元数据；
2. 使用 `RecursiveCharacterTextSplitter` 完成文本切分；
3. 为每个文本块保存 `document_id`、`chunk_id`、`page` 和 `source`；
4. 调用 Embedding 模型生成向量；
5. 将文本块、向量和元数据写入 Milvus Lite；
6. 完成文档删除和重新索引功能；
7. PDF 流程稳定后，再增加 DOCX、Markdown 和 TXT。

```text
文档 → 解析正文 → 按页保留来源 → 文本切块 → Embedding → Milvus Lite
```

**阶段产出：**独立的文档入库程序。

**验收标准：**导入一篇 PDF 后，可以查看切分结果，并能通过语义查询得到相关文本块及正确页码。

### 第三阶段：实现基础 RAG（3～4 天）

**开发内容：**

1. 实现 `search_documents(query, knowledge_base_id, top_k)`；
2. 将检索结果整理为带编号的上下文；
3. 编写只允许根据检索证据回答的 Prompt；
4. 生成回答时保留 `[来源 1]` 等引用编号；
5. 将引用编号映射到文档名、页码和原文片段；
6. 对没有相关证据的问题明确返回“当前知识库中没有足够信息”。

```text
问题 → 向量检索 → Top-K 文本块 → 拼接上下文 → LLM 回答 → 引用映射
```

**阶段产出：**一个不依赖 Agent 的基础 RAG 问答程序。

**验收标准：**能够回答单篇和多篇文档中的问题；每个重要结论都能定位到真实文档和页码。

### 第四阶段：升级为 Agentic RAG（约 1 周）

基础 RAG 稳定后，再引入 LangGraph。不要一开始就让 Agent 控制全部流程。

**开发内容：**

1. 定义 `AgentState`，保存用户问题、子问题、检索结果、证据状态和最终回答；
2. 实现意图识别节点，区分普通对话、文档问答、文献比较、笔记和报告任务；
3. 实现问题规划节点，将复杂问题拆分为多个子问题；
4. 将 `search_documents`、`list_documents` 和 `get_document_content` 封装为工具；
5. 实现检索节点，并发执行多个子问题；
6. 实现证据充分性判断节点；
7. 证据不足时改写查询并再次检索，最多重试一次或两次；
8. 实现答案生成和引用检查节点。

```text
START
  ↓
意图识别 → 是否需要检索？ ──否──→ 普通回答
  ↓是
问题规划 → 文档检索 → 证据是否充分？
                         ├─ 否 → 改写问题 → 再次检索
                         └─ 是 → 生成答案 → 引用检查 → END
```

**阶段产出：**能够自主规划、检索和修正查询的 LangGraph 工作流。

**验收标准：**可以比较多篇论文；证据不足时确实会进行第二次检索；循环次数存在上限。

### 第五阶段：加入对话与长短期记忆（3～5 天）

**开发内容：**

1. 使用 `thread_id` 标识不同会话；
2. 接入 LangGraph Checkpointer 保存消息和工作流状态；
3. 支持根据相同 `thread_id` 恢复对话；
4. 实现 `save_note`，只在用户明确要求时保存研究笔记；
5. 实现 `search_notes`，通过语义检索复用历史笔记；
6. 长期记忆保存研究方向、用户偏好和经过确认的结论，不保存所有原始对话；
7. 确保不同用户和不同会话之间的数据相互隔离。

**阶段产出：**短期对话记忆和长期研究笔记两套独立机制。

**验收标准：**重新启动服务后可以继续历史会话；新会话不会错误继承旧会话上下文；保存的笔记可以再次检索。

### 第六阶段：开发 FastAPI 接口（3～5 天）

**开发内容：**

1. 知识库创建、查询和删除接口；
2. 文档上传、状态查询、删除和重新索引接口；
3. 会话创建、历史查询和删除接口；
4. 聊天接口和 Agent 调用接口；
5. 通过 SSE 流式返回模型内容、工具调用状态和最终引用；
6. 增加 Pydantic 请求校验、统一响应结构和异常处理；
7. 生成并检查 OpenAPI 接口文档。

**阶段产出：**前端可以调用的完整后端服务。

**验收标准：**使用 Swagger 或 Postman 可以完成“创建知识库—上传文档—提问—追问—查看引用”的完整流程。

### 第七阶段：开发网页前端（约 1 周）

**开发内容：**

1. 使用 Vue 3 建立项目；
2. 开发知识库和文档管理页面；
3. 开发会话列表和聊天页面；
4. 使用 SSE 展示流式回答；
5. 展示 Agent 当前状态，例如正在规划、检索或生成答案；
6. 点击引用后展示文档名、页码和原文片段；
7. 增加研究笔记和历史报告页面。

**阶段产出：**可演示的多文档研究助理网页。

**验收标准：**用户不需要使用命令行，即可完成文档上传、问答、追问、笔记保存和引用查看。

### 第八阶段：检索增强、评价与部署（约 1 周）

**开发内容：**

1. 将 Milvus Lite 切换为通过 Docker 部署的 Milvus Server；
2. 在 Milvus 稠密向量检索基础上增加 BM25 稀疏检索；
3. 使用 RRF 融合两路结果，再使用 Cross-Encoder 重排序；
4. 建立约 30～50 条人工问答与引用测试数据；
5. 评价检索命中率、回答忠实度和引用正确率；
6. 增加日志、超时、重试、限流和失败降级；
7. 编写单元测试、接口测试和关键工作流测试；
8. 使用 Docker Compose 部署后端、前端、Milvus 和关系数据库；
9. 完善 README、架构图、接口文档和演示视频。

**阶段产出：**可以写入简历并进行演示的工程化项目。

**验收标准：**项目可通过一条部署命令启动；核心测试能够通过；README 能让其他人独立运行项目。

### 8.1 推荐的最小迭代节点

如果希望尽快看到成果，可以按下面三个版本提交 Git 标签：

| 版本 | 包含阶段 | 可以展示的能力 |
| --- | --- | --- |
| `v0.1-basic-rag` | 第一至第三阶段 | 上传文档、语义检索、带页码引用的问答 |
| `v0.2-agentic-rag` | 第四至第五阶段 | 查询规划、二次检索、多轮对话、长短期记忆 |
| `v1.0-web-app` | 第六至第八阶段 | 完整网页、流式输出、混合检索、评价和部署 |

### 8.2 开发过程中的优先级

```text
引用正确性 > 检索效果 > Agent 自主性 > 记忆 > 网页样式
```

先确保检索结果和引用可靠，再增加 Agent 决策与界面。网页做得漂亮，但检索和引用不准确，不能体现这个项目最核心的价值。

## 9. 评价方案

本项目不计算 DTI 的 AUROC、AUPRC 等预测指标，主要评价检索、回答和工程性能。

### 9.1 检索质量

- Recall@K、Precision@K、MRR、NDCG；
- 正确文档和正确页面命中率。

### 9.2 回答质量

- Faithfulness：回答是否忠于证据；
- Answer Relevance：回答是否切中问题；
- Context Relevance：检索内容是否相关；
- Citation Accuracy：引用是否正确；
- Unsupported Claim Rate：无证据结论比例。

### 9.3 Agent 能力

- 工具选择正确率；
- 不必要检索比例；
- 二次检索成功率；
- 任务完成率；
- 长短期记忆命中率。

### 9.4 工程指标

- 平均响应时间和首个 Token 延迟；
- 平均工具调用次数及 Token 消耗；
- 文档索引时间；
- 接口成功率和异常恢复率。

## 10. 第一版范围控制

第一版暂不实现：

- DTI 模型调用及专业生物数据库；
- 多 Agent 协作；
- 复杂用户权限；
- Redis 和任务队列；
- 多种向量数据库；
- Kubernetes；
- 本地部署大型语言模型；
- 复杂前端动画。

第一版只需要完成：

```text
文档上传和解析
+ Milvus Lite 向量知识库
+ 一个检索工具
+ 一个 LangChain Agent
+ 多轮对话与引用
+ 一个简单 FastAPI 服务
```

## 11. 后续与 DTI 研究结合（可选）

通用版本完成后，不需要推倒重写，可以增加 DTI 领域适配层：

- 将通用知识库扩充为 DTI 文献知识库；
- 增加药物、靶标和相互作用实体识别；
- 增加 UniProt、STRING、PubMed 和 ChEMBL 查询工具；
- 从知识丰富的头部靶标中检索适合尾部靶标的可迁移知识；
- 生成支持证据、反对证据、知识缺口和实验建议；
- 评价知识检索、引用正确性、知识迁移有效率和尾部知识覆盖率。

这一扩展定位为“长尾 DTI 的知识迁移与证据分析”，不必直接调用 DTI 模型，也不把语言模型生成的分数称为预测概率。

## 12. 预期成果

- 一个可以独立运行的 Python Agent 项目；
- 一套 FastAPI 接口；
- 一个支持多轮对话的网页；
- 一个多文档 RAG 知识库；
- 一组研究工具；
- 短期对话记忆和长期研究笔记；
- 流式回答与引用溯源；
- 一套 RAG 和 Agent 评价脚本；
- Docker 部署文件；
- README、架构图、接口文档和演示视频。

## 13. 简历表述参考

**基于 LangChain 与 LangGraph 的多文档研究助理 Agent**

- 基于 LangChain 1.x 与 LangGraph 构建研究助理 Agent，实现工具调用、多轮对话、条件路由和状态化工作流。
- 构建支持 PDF、DOCX 和 Markdown 的多文档知识库，结合向量检索、BM25 与 Cross-Encoder 完成混合检索和重排序。
- 实现查询改写、检索充分性判断、二次检索和引用校验，返回文档、页码及原文片段，降低无依据回答。
- 使用 Checkpointer 和向量存储分别实现短期对话记忆与长期研究笔记记忆。
- 基于 FastAPI 与 SSE 实现流式对话接口，并通过测试、日志和 Docker 完成项目工程化。
